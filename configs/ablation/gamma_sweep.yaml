# γ sweep — stability–plasticity tradeoff (Proposition 2 ablation)
# γ → 1: more stability (protect old tasks), γ → 0: more plasticity (learn new)
ablation:
  sweep_param: "method.gamma"
  values: [0.0, 0.25, 0.5, 0.75, 1.0]
  base_method: "kbezier"
  seeds: [1, 2, 3, 4, 5]
